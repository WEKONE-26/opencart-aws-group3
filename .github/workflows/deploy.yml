name: Deploy OpenCart to ASG (DISABLED - Manual Only)

on:
  workflow_dispatch:  # Manual trigger ONLY - No auto-deploy

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'CI/CD is disabled for auto-deploy. Manual trigger only via Actions tab'
